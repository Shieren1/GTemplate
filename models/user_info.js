// const db = require('../config/db');
// const user_info = {
//     save:(data, callback) =>{
//         const query ="insert into user_info (first_name, last_name, email) values (?,?,?)";
//         db.query(query,  [data.first_name, data.last_name, data.email], callback);
//     }

// };
// module.exports = user_info;

